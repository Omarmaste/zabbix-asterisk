# Crear .gitignore en la raíz del repo
cat > .gitignore <<'EOF'
# State files - no commitear
agent_*_state.json
agent_*_batch.txt
agent_*_curl.json

# Logs
*.log

# Credenciales locales
.env
config.local.*
EOF
